@page
@model FridayFilmClub.Pages.SignInModel
@{
    ViewData["Title"] = "Sign In";
}

<br>
<div class="login">
    <div class="login-container">
        <h2 class="text-center" style="margin-bottom: 20px;">Sign Up</h2>

        @if (ModelState.ContainsKey(string.Empty) && ModelState[string.Empty]?.Errors?.Count > 0)
        {
            <div class="alert alert-danger">
                @if (ModelState[string.Empty]?.Errors != null)
                {
                    @foreach (var error in ModelState[string.Empty]?.Errors ?? Enumerable.Empty<Microsoft.AspNetCore.Mvc.ModelBinding.ModelError>())
                    {
                        <p>@error.ErrorMessage</p>
                    }
                }
            </div>
        }

        <form method="post">
            <div class="form-group">
                <input type="text" class="form-control" id="Username" name="Username" placeholder="Username" required maxlength="15" />
            </div>
            <div class="form-group">
                <input type="password" class="form-control" id="Password" name="Password" placeholder="Password" required />
                <small class="text">Password must be above 8 characters long.</small>
            </div>
            <div class="form-group">
                <input type="password" class="form-control" id="ReEnterPassword" name="ReEnterPassword" placeholder="Re-enter Password" required />
            </div>
            <button type="submit" class="btn btn-primary btn-block">Sign In</button>
        </form>
    </div>
</div>